# Installed inside the image by `npm install`; copying the host tree bloats the
# build context by ~38MB and can carry platform-specific binaries.
node_modules/

# Build output, regenerated in the container.
dist/
.vite/

.git/
.gitignore
.env
.env.local
